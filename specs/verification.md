# Verification specification

Verification is independent of AI and uses an explicit project verification
record. Supported states are `UNVERIFIED`, `PARTIALLY_VERIFIED`, `VERIFIED`,
`STALE`, and `DISPUTED`, with an optional verification date, notes, and source
record. The demonstration dataset is explicitly `UNVERIFIED`; no fictional
record is presented as independently verified.
