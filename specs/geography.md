# Geography specification

The navigation hierarchy is Country → County → Ward → Project. A future
geography module will add Country, County, SubCounty, Ward, and Location models
with PostGIS geometry where spatial boundaries or coordinates are required.
